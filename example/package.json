{
  "name": "instantdb-flutter-example",
  "version": "1.0.0",
  "description": "InstantDB schema management for Flutter example app",
  "scripts": {
    "schema:push": "instant-cli push",
    "schema:pull": "instant-cli pull-schema",
    "schema:validate": "tsc --noEmit instant.schema.ts"
  },
  "dependencies": {
    "@instantdb/react": "^0.21.5"
  },
  "devDependencies": {
    "instant-cli": "^0.21.5",
    "typescript": "^5.0.0"
  },
  "type": "module"
}